<%-include ../partials/header%>

<div class="container-fluid">
  <div class="row">
    <%-include ../partials/sidebar%>
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
      <!-- <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Dashboard</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
          <div class="btn-group mr-2">
            <button type="button" class="btn btn-sm btn-outline-secondary">Share</button>
            <button type="button" class="btn btn-sm btn-outline-secondary">Export</button>
          </div>
          <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle">
            <span data-feather="calendar"></span>
            This week
          </button>
        </div>
      </div> -->

      <h2 class="pt-3">Stores List <a href="/stores/create" class="btn btn-primary btn-sm float-right">Add Store</a></h2>
      <div class="table-responsive">
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th>#</th>
              <th>Store Name</th>
              <th>Status</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if(data.length){ %>
              <% data.forEach((item) => { %>
                    <tr>                                              
                        <td><%=item.id%></td>
                        <td><%=item.name%></td>
                        <td><%=item.active%></td>
                        <td><a href="../stores/edit/<%=item.id%>"><i class="fa fa-pencil text-success"></i>Edit</a> &nbsp;|&nbsp;<a href="../stores/delete/<%=item.id%>"><i class="fa fa-trans text-danger"></i>Delete</a></td>
                    </tr>
                  <% }); %>   
              <% } %>  
          </tbody>
        </table>
      </div>
    </main>
  </div>
</div>

<%-include ../partials/footer%>